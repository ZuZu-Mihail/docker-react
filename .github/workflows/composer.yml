name: Docker Composer CI
 
on:
  push:
    branches: [ "master" ]



jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: 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
          publish_dir: _site
      - uses: actions/checkout@v3
      - name: Build Task-Scheduler-App comp
        run: docker-compose up --build
